# 示例 04: HPC 部署

## 场景说明

演示如何将计算任务提交到 HPC 集群（SLURM/K8s）并管理作业生命周期。

**场景**: 在 SLURM 集群上运行大规模分子动力学模拟（100 万原子，100ns 时间尺度）。

**涉及工具**: `env_detect` → `exp_plan` → `exp_run` → HPC 提交 → `exp_monitor` → 结果收集

**预计时间**: 30 分钟准备 + 集群排队时间
