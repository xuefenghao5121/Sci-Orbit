{
  "name": "@ai4s/monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["packages/*"],
  "description": "Sci-Orbit - AI for Scientific Computing Orchestrator",
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces --parallel",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "benchmark": "cd tests/benchmark && npm install && npm test"
  }
}
